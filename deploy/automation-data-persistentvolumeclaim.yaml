apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: automation-data
  name: automation-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
