apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: automationhangfireapi-hpa
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: automationhangfireapi
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50  # Scale when average CPU utilization exceeds 50%